# ADD — Light tech palette (ticket 003)

Invert `ink` / `paper` / `panel` / `mist` / `line` in `tailwind.config.ts`.
Cobalt stays. Buttons on cobalt use `text-white`.
Hero becomes a split layout so photographs sit as objects on light, not full-bleed night.
Copy: kicker + em-dash cleanup in `app/` and `data/copy.ts`.
